<main>
  <div class="container">
    <div class="row">
      <div class="col-2 my-4 px-3">
        <table>
          <thead>
            <tr>
              <td>タグ検索</td>
            </tr>
          </thead>
          <tbody>
            <% @tags.each do |t| %>
              <tr>
                <td>
                  <%= link_to t.name, posts_path(tag_id: t.id) %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
      <div class="col-10 my-4 px-3">
        <h2>ALL</h2>
        <div class="card-box">
          <% @posts.each do |p| %>
            <div class="card">
              <%= link_to post_path(p.id) do %>
                <%# image_tag p.post_images.thumb.url, size: "100x100" %>
              <% end %>
              <div class="card-body">
                <p class="card-title"><%= p.title %></p>
                <div class="card-body__link">
                  <%= link_to favorites_path(post_id: p.id), method: :delete do %>
                    <i class="fas fa-heart fa-pink"></i>
                    <%= p.favorite_users.count %>
                  <% end %>
                  <%= link_to favorites_path(post_id: p.id), method: :post do %>
                    <i class="far fa-heart fa-pink"></i>
                    <%= p.favorite_users.count %>
                  <% end %>
                  <i class="fas fa-comment fa-blue"></i>
                  <%= p.comments.count %>
                  <i class="fas fa-paw fa-brown"></i>
                  <%= p.footprint %>
                </div>
                <div class="user-body px-4">
                  <%= link_to user_path(p.user_id) do %>
                    <%= image_tag p.user.user_image.thumb.url, size: "70x70", class: "user-icon" %>
                  <% end %>
                  <div class="user-body__text">
                    <p><%= p.user.family_name + p.user.given_name %></p>
                    <div class="follow">
                      <%= link_to "フォロー", relationships_path, method: :post, class: "btn_blue" %>
                      <%= link_to "フォローを外す", relationships_path, method: :destroy, class: "btn_red" %>
                    </div>
                    <div class="bookmark">
                      <%= link_to "ブックマーク", bookmarks_path, method: :post, class: "btn_blue" %>
                      <%= link_to "ブックマークを外す", bookmarks_path, method: :destroy, class: "btn_red" %>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</main>