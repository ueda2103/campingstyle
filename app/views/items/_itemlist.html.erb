<% items.each do |i| %>
  <%= link_to i.name, item_path(i.id), method: :patch, remote: true, class: "btn_blue" %>
  <%= i.status %>
  <%= link_to "削除", item_path(i.id), method: :delete, remote: true, class: "btn_red" %>
<% end %>
